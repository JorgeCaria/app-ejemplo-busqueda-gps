import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-ejemplo-busqueda-gps',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
